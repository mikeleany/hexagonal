# Additional words for scripts/generateWordLists.py to block beyond what
# SCOWL's usage-note tags and the LDNOOBW list already catch (milder
# profanity, or words neither source tags at all).
# One lowercase word per line. '#' starts an inline comment. Blank lines
# ignored. scripts/wordWhitelist.txt wins over this file, so a word never
# needs to be removed from here just because it's also whitelisted.
#
# Review .cache/scowl/filtered-offensive.txt (generated by that script) to
# decide what belongs here.
asses
