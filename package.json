{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "npm run words:generate && npm run puzzles:generate:local",
    "dev": "vite",
    "prebuild": "npm run words:generate",
    "build": "vite build",
    "preview": "vite preview",
    "precheck": "npm run words:generate",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json",
    "pretest": "npm run words:generate",
    "test": "vitest run",
    "prebench": "npm run words:generate",
    "bench": "vitest bench",
    "words:generate": "python3 scripts/generateWordLists.py",
    "puzzles:generate:local": "vite-node scripts/generatePuzzles.ts --days 2",
    "puzzles:generate": "vite-node scripts/generatePuzzles.ts",
    "puzzles:remove-word": "vite-node scripts/removeWord.ts"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "@tsconfig/svelte": "^5.0.8",
    "@types/node": "^24.13.3",
    "svelte": "^5.56.8",
    "svelte-check": "^4.7.3",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vite-node": "^6.0.0",
    "vitest": "^4.1.10"
  }
}
